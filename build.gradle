buildscript {
  repositories {
    mavenCentral()
  }

  dependencies {
    classpath 'com.android.tools.build:gradle:0.12.1'
  }
}

allprojects {
  group = 'net.simonvt.numberpicker'
  version = '1.0.1-INLOOP'

  repositories {
    mavenCentral()
  }

  tasks.withType(Compile) {
    options.encoding = "UTF-8"
  }
}
